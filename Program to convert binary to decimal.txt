#include <stdio.h>
#include <math.h>
#include <string.h>
void main() {
    int ele;
    printf("Enter no. for conversion:\n 1 - Decimal to Binary\n 2 - Binary to Decimal");
    scanf("%d", &ele);
    if(ele == 1){
        converta();
    }
    else if(ele == 2){
        convertb();
    }
    else{
        printf("Enter a valid conversion!");
    }
}
void converta(){
    long int bi = 0; int n, d, g, i = 0;
    printf("Enter no. in decimal form:");
    scanf("%d", &n);
    while(n > 0){
        d = n % 2;
        n /= 2;
        g=pow(10,i);
        i++;
        bi += g * d;
    }
    printf("Binary form is: %ld", bi);
    //return(bi);
}
