#include <stdio.h>
#include <math.h>

int main(){
    int i, j, n, mul;
    printf("Enter the no. til you what to print its table:");
    scanf("%d", &n);
    printf("The table's are:\n");
    for(i = 1; i <= n; i++){
        printf("-----------------------------\n");
        for(j = 1; j <= 10; j++){
            printf("%d * %d is %d\n", i, j, i*j);
        }
        printf("-----------------------------\n");
        printf("\n");
    }
    
    return 0;
}