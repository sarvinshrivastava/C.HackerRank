#include <stdio.h>
#include <math.h>

int main() {
    int n, a, g = 0, i;
    scanf("%d\n", &n);
    for(i = 1; i <= n; i++){
        scanf("%d\n", &a);
        if(g < a){
            g = a;
        }
    }
    printf("Biggest is %d", g);
    return 0;
}